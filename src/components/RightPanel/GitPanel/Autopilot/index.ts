// Autopilot's two footprints in the Git panel: the kill switch (status header)
// and the receipt of what it did (scrollable body). Autopilot itself is how the
// agent behaves on a PR — see `@/autopilot` — not a feature the panel advertises.
export { AutopilotHistory } from "./AutopilotHistory";
export { AutopilotSwitch } from "./AutopilotSwitch";
