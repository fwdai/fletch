// Pi's `agent_end` is surfaced as a `turn_end` notice, which (like the others)
// stays hidden — so the shared default applies unchanged.
export { DEFAULT_POLICY as piPolicy } from "@/adapters/shared/default-policy";
