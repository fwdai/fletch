// Shared with the desktop, which browses a paired host's disk with the same
// arithmetic (src/util/paths.ts). Re-exported here so the phone's imports read
// the way every other local helper does.

export { baseName, childPath, parentPath } from "@desktop/util/paths";
